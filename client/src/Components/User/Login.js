import React from "react";
// import { useLocation } from "react-router-dom";

export default function Login(props) {
  //   const location = useLocation();

  return (
    <div>
      <h2>this is to redirect to login page</h2>
    </div>
  );
}
